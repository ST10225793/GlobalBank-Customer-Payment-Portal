# Banking Payment System

## Prerequisites
- Node.js (v18 or higher)
- npm (v9 or higher)

## Setup Instructions

### 1. Clone and setup backend
```bash
git clone <your-repo>
cd banking-payment-system/backend
cp .env.example .env  # Create .env file
npm install
npm run dev